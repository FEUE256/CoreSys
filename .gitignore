# A file with all the content of all files generated by:
# find . -type f -exec cat {} + > out.txt
src/CoreSys/kernel/out.txt
# db.exe
**/*.exe
#find . -type f   ! -path "./bin/*"   ! -path "./.git/*"   ! -path "./misc/*"   ! -path "./build/linux/utils/*"   ! -name "out.txt"   ! -name "*.pdf" ! -name "*.png" ! -path "./firmware/*"  -exec sh -c 'echo "===== $1 ====="; cat "$1"; echo' sh {} \; > out.txt
out.txt 
# find . -type f -exec cat {} + > out.txt
full.txt
kernel.mk
